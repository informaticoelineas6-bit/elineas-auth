# Despliegue de producción: sin Postgres local, la API se conecta a Neon.
# Uso: docker compose -f docker-compose.prod.yml up -d --build
services:
  api:
    build:
      context: .
      target: prod
    image: elineas-auth:latest
    restart: unless-stopped
    env_file:
      - .env.production
    environment:
      APP_ENV: production
    ports:
      - "8080:8080"
